machine:
  network:
    hostname: worker-1
    interfaces:
      - deviceSelector:
          physical: true
        addresses:
          - 192.168.99.111/24
        routes:
          - network: 0.0.0.0/0
            gateway: 192.168.99.1
  install:
    disk: /dev/mmcblk0
    image: factory.talos.dev/installer/${SCHEMATIC_ID}:v1.11.5
